---
- name: My System | Provision all required infrastructure
  hosts: localhost
  connection: local
  gather_facts: no
  tasks:
    - name: Create new customer gateway
      ec2_vpc_vpn:
        state: present
        region: us-east-1
        vpn_gateway_id: vgw-0b2749bcec21edc1a
        customer_gateway_id: cgw-0e2956b0d830cd440
      register: vpn_gateway_id
  
